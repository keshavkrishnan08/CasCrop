# Row 4: Symmetric ECMP — ECMP with single transformation for all shocks
# Tests whether asymmetric pos/neg shock treatment matters
_base_: default.yaml

model:
  name: symmetric_ecmp
  use_economic: true
  use_graph: true
  graph_type: full
  use_ecmp: true
  ecmp_asymmetric: false
  use_disentanglement: true
